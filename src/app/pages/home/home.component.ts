import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-home',
  templateUrl: './home.component.html',
  styleUrls: ['./home.component.scss'],
})
export class HomeComponent implements OnInit {
  constructor() {
    // Inicializações que você deseja fazer no construtor
  }

  ngOnInit(): void {
    // Inicializações que você deseja fazer ao inicializar o componente
  }
}
